{
  "name": "@volta-oss/student-pod-playbook",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "A repeatable 3-4 student delivery model from kickoff to handoff.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/student-pod-playbook:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "digital-equity",
    "local-first",
    "oss",
    "student agency operations",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/student-pod-playbook.git"
  },
  "homepage": "https://volta-npo.github.io/student-pod-playbook/",
  "bugs": {
    "url": "https://github.com/volta-npo/student-pod-playbook/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^25.9.1"
  }
}
